<!DOCTYPE html>
<html>
<head>
  <title>Home Page</title>
 
</head>
<body class="bg-light">
  <div class="container mt-5">
    <h1 class="mb-4">Form Example</h1>
    
    <form action="/home" method="POST" class="card p-4 shadow-sm">
      <div class="mb-3">
        <input type="text" name="in1" class="form-control" placeholder="Enter input 1" />
      </div>
      <div class="mb-3">
        <input type="text" name="in2" class="form-control" placeholder="Enter input 2" />
      </div>
      <button type="submit" class="btn btn-primary">Submit</button>
    </form>

    <% if (in1 || in2) { %>
      <div class="alert alert-info mt-4">
        <% if (in1) { %> <p>Input 1: <strong><%= in1 %></strong></p> <% } %>
        <% if (in2) { %> <p>Input 2: <strong><%= in2 %></strong></p> <% } %>
      </div>
    <% } %>
  </div>

  <!-- Bootstrap JS (optional) -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
